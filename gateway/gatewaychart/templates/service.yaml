apiVersion: v1
kind: Service
metadata:
  name: {{ include "gatewaychart.fullname" . }}
  labels:
    {{- include "gatewaychart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gatewaychart.selectorLabels" . | nindent 4 }}
